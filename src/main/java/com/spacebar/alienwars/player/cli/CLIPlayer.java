package com.spacebar.alienwars.player.cli;

import com.spacebar.alienwars.player.AbstractPlayer;
import com.spacebar.alienwars.player.PlayerType;

public class CLIPlayer extends AbstractPlayer {

    public CLIPlayer(PlayerType playerType, String playerName) {
        super(playerType, playerName);
    }
}
